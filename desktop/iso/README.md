# Desktop ISO Planning

This directory is reserved for future Linux-based Liam_OS Desktop ISO build configuration.

Possible build systems:

- Debian `live-build`.
- Ubuntu remix pipeline.
- Cubic for early experiments only.
- Calamares for installer customization.

Do not place custom-kernel boot files here. The custom kernel remains under `core/`.
